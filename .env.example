# Supabase project (same backend as the BreakQ app)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Where "Become a partner" applications get emailed.
# Only used server-side (Supabase Edge Function) — never read by the browser bundle.
PARTNER_NOTIFY_EMAIL=bjain5329@gmail.com
